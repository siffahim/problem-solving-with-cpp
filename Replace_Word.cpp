#include <bits/stdc++.h>
using namespace std;

int main(){
    string s, x;
    cin >> s >> x;

    for (string::iterator it = s.begin(); it < s.end(); it++){
        cout << *it << endl;
    }

    return 0;
}